name: Build Extension Zip
on:
  workflow_call:
    inputs:
      extensionName:
        description: "Your extension name"
        required: true
        type: string
      path:
        description: "Path to your bundle"
        required: false
        type: string
        default: "."
jobs:
  run:
    name: Build and Validate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Install onlishop-cli
        uses: onlishop/onlishop-cli-action@v2
      - name: Build
        run: onlishop-cli extension zip "${{ inputs.path }}" --git-commit "${{ github.sha }}" --release
      - name: Rename
        run: mv ${{ inputs.extensionName }}-${{ github.sha }}.zip ${{ inputs.extensionName }}.zip
      - name: Validate
        run: onlishop-cli extension validate ${{ inputs.extensionName }}.zip
      - name: Upload artifact
        uses: actions/upload-artifact@v5
        with:
          name: ${{ inputs.extensionName }}
          path: ${{ inputs.extensionName }}.zip
